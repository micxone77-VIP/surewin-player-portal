name: Portal CI

on:
  push:
    branches:
      - main
      - feature/portal-p0-final
      - feature/portal-activation-link
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run activation link tests
        run: node --test activation.test.mjs

      - name: Run regression tests
        run: node --test portal-security-regression.test.mjs

      - name: Build production bundle
        run: npm run build
