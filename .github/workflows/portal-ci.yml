name: Portal CI

on:
  push:
    branches:
      - feature/campaign-portal-p0
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run regression tests
        run: node --test reward-regression.test.mjs portal-security-regression.test.mjs

      - name: Build production bundle
        run: npm run build
